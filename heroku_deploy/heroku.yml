setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: heroku_deploy/Dockerfile
run:
  web: java -Dspring.profiles.active=heroku -Dserver.port=$PORT -jar /app/app.jar